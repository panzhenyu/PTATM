#include <stdlib.h>

int main() {
	int a = 1, b = 2;
	if (a) {
		++b;
	} else {
		b -= a;
	}

	for (int i=0; i<100; ++i) {
		a += i;
		b -= i;
	}

	if (b < 0) {
		a += b;
	} else {
		b += a;
	}

	for (int i=0; i<100; ++i) {
		a += i;
		b -= i;
	}

	if (b < 0) {
		a += b;
	} else {
		b += a;
	}
	
	for (int i=0; i<100; ++i) {
		a += i;
		b -= i;
	}

	if (b < 0) {
		a += b;
	} else {
		b += a;
	}

	
	for (int i=0; i<100; ++i) {
		a += i;
		b -= i;
	}

	if (b < 0) {
		a += b;
	} else {
		b += a;
	}

	
	for (int i=0; i<100; ++i) {
		a += i;
		b -= i;
	}

	if (b < 0) {
		a += b;
	} else {
		b += a;
	}

	
	for (int i=0; i<100; ++i) {
		a += i;
		b -= i;
	}

	if (b < 0) {
		a += b;
	} else {
		b += a;
	}

	
	for (int i=0; i<100; ++i) {
		a += i;
		b -= i;
	}

	if (b < 0) {
		a += b;
	} else {
		b += a;
	}

	
	for (int i=0; i<100; ++i) {
		a += i;
		b -= i;
	}

	if (b < 0) {
		a += b;
	} else {
		b += a;
	}

	
	for (int i=0; i<100; ++i) {
		a += i;
		b -= i;
	}

	if (b < 0) {
		a += b;
	} else {
		b += a;
	}

	
	for (int i=0; i<100; ++i) {
		a += i;
		b -= i;
	}

	if (b < 0) {
		a += b;
	} else {
		b += a;
	}

	
	for (int i=0; i<100; ++i) {
		a += i;
		b -= i;
	}

	if (b < 0) {
		a += b;
	} else {
		b += a;
	}

	
	for (int i=0; i<100; ++i) {
		a += i;
		b -= i;
	}

	if (b < 0) {
		a += b;
	} else {
		b += a;
	}

	
	for (int i=0; i<100; ++i) {
		a += i;
		b -= i;
	}

	if (b < 0) {
		a += b;
	} else {
		b += a;
	}

	
	for (int i=0; i<100; ++i) {
		a += i;
		b -= i;
	}

	if (b < 0) {
		a += b;
	} else {
		b += a;
	}

	
	for (int i=0; i<100; ++i) {
		a += i;
		b -= i;
	}

	if (b < 0) {
		a += b;
	} else {
		b += a;
	}

	
	for (int i=0; i<100; ++i) {
		a += i;
		b -= i;
	}

	if (b < 0) {
		a += b;
	} else {
		b += a;
	}

	
	for (int i=0; i<100; ++i) {
		a += i;
		b -= i;
	}

	if (b < 0) {
		a += b;
	} else {
		b += a;
	}

	
	for (int i=0; i<100; ++i) {
		a += i;
		b -= i;
	}

	if (b < 0) {
		a += b;
	} else {
		b += a;
	}

	
	for (int i=0; i<100; ++i) {
		a += i;
		b -= i;
	}

	if (b < 0) {
		a += b;
	} else {
		b += a;
	}

	
	for (int i=0; i<100; ++i) {
		a += i;
		b -= i;
	}

	if (b < 0) {
		a += b;
	} else {
		b += a;
	}

	exit(0);

	return 0;
}
